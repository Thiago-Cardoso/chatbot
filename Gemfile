source :rubygems
gem 'sinatra' #framework ruby
gem "activerecord" #active record for database
gem "sinatra-activerecord"
gem 'pg' #postgree
gem 'rake' #migration
gem 'pg_search' #search

group :development do
 gem "tux" #rails console
end

group :test do
  #help with require
  gem "rack-test", require: "rack/test"
  gem "rspec" #test
  gem "factory_bot"
  gem "ffaker"
  gem 'database_cleaner'
end
